{
  "name": "template-repository", // TODO: Modify to match the repository name
  "dockerComposeFile": "docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspaces/template-repository", // TODO: Modify to match the repository name
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {},
    "./local-features/just": {},
    "./local-features/go": {},
    "./local-features/node": {}
  },
  "postCreateCommand": "npm install -g @anthropic-ai/claude-code && npm install -g prettier && npm install -g baedal",
  "customizations": {
    "vscode": {
      "extensions": ["esbenp.prettier-vscode"],
      "settings": {
        "editor.defaultFormatter": "esbenp.prettier-vscode"
      }
    }
  },
  "remoteUser": "root"
}
